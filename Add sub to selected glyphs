# print names in selected glyphs adding a sufix

## select names
select = Glyphs.font.selectedLayers

## print names adding a sufix in string: 'sub'

for lista in select:
    print lista.parent.name +'_sub'
